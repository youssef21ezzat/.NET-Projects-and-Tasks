@*
    Views/Students/Demo.cshtml — PRE-TYPED, NOT TODAY'S TOPIC.

    This page exists only so that TODO 1's LAST branch — the one that
    renders a page — has somewhere to land. It takes no model at all,
    which is itself the point being made: View() with no argument is a
    perfectly ordinary thing for an action to return.
*@
@{
    ViewData["Title"] = "Demo";
}

<h1 class="display-5">This one is a View.</h1>
<p class="lead">
    Four other values of <code>id</code> produced four completely different
    kinds of answer from the same method. This is the fifth.
</p>
<a href="/students">Back to all students</a>
